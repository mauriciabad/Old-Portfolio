---
label: Landing
hide_body: true
fields:
  - name: projects
    type: field_group_list
    fields:
      - name: project
        type: include
        config: {}
        template: project
        label: Project
    config:
      min:
      max:
      labelField:
    label: Projects
pages:
  - docs/landing.json
